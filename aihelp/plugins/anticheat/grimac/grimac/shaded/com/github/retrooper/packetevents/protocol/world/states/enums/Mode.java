package ac.grim.grimac.shaded.com.github.retrooper.packetevents.protocol.world.states.enums;

public enum Mode {
   COMPARE,
   SUBTRACT,
   SAVE,
   LOAD,
   CORNER,
   DATA,
   START,
   LOG,
   FAIL,
   ACCEPT;

   // $FF: synthetic method
   private static Mode[] $values() {
      return new Mode[]{COMPARE, SUBTRACT, SAVE, LOAD, CORNER, DATA, START, LOG, FAIL, ACCEPT};
   }
}
