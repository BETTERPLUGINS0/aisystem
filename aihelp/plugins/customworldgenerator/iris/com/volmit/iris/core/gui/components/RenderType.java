package com.volmit.iris.core.gui.components;

public enum RenderType {
   BIOME,
   BIOME_LAND,
   BIOME_SEA,
   REGION,
   CAVE_LAND,
   HEIGHT,
   OBJECT_LOAD,
   DECORATOR_LOAD,
   CONTINENT,
   LAYER_LOAD;

   // $FF: synthetic method
   private static RenderType[] $values() {
      return new RenderType[]{BIOME, BIOME_LAND, BIOME_SEA, REGION, CAVE_LAND, HEIGHT, OBJECT_LOAD, DECORATOR_LOAD, CONTINENT, LAYER_LOAD};
   }
}
